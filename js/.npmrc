# Use hoisted node_modules layout for React Native / Metro bundler compatibility.
# Metro doesn't support pnpm's default symlinked .pnpm store structure.
# https://docs.npmjs.com/cli/v10/using-npm/config#node-linker
node-linker=hoisted
