---
'@fedimint/fedimint-client-wasm-bundler': patch
'@fedimint/fedimint-client-wasm-web': patch
---

Rebuild the wasm client from the fedimint revision the Rust SDK now compiles against, so every client in this repo and the federation they are tested against come from one commit
