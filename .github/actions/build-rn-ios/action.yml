name: Build iOS RN
description: Builds iOS React Native bindings using Nix-cached Rust derivations
inputs:
  device-only:
    description: |
      Build only the aarch64-apple-ios (device) target instead of all three
      iOS Rust targets. Roughly halves the iOS Rust cross-compile time on
      macos-latest, suitable for PR smoke tests. Release / publish flows
      should leave this `false` so the resulting .xcframework includes the
      simulator slices.
    required: false
    default: 'false'
outputs:
  sha256:
    description: 'SHA256 of the ios-artifacts.zip'
    value: ${{ steps.compress.outputs.sha256 }}
runs:
  using: composite
  steps:
    - name: Build iOS via Nix
      # iOS Nix derivations are impure (read /Applications/Xcode.app), so the
      # daemon must allow relaxed sandboxing. `just build-ios` invokes
      # `nix build` for each iOS Rust target then runs ubrn --no-cargo to
      # assemble the .xcframework.
      shell: bash
      env:
        IOS_TARGETS: ${{ inputs.device-only == 'true' && 'aarch64-apple-ios' || '' }}
      run: nix develop --accept-flake-config -c just build-ios

    - name: Push FFI libs to Cachix
      # Same rationale as the Android action: cachix-action's post-run hook
      # only pushes newly *built* paths, so already-realized libs never reach
      # the cache. Push explicitly so local builds can substitute them. The
      # fallback target list mirrors nix-prebuild.sh's default.
      shell: bash
      continue-on-error: true
      env:
        IOS_TARGETS: ${{ inputs.device-only == 'true' && 'aarch64-apple-ios' || '' }}
      run: |
        installables=()
        for target in ${IOS_TARGETS:-aarch64-apple-ios aarch64-apple-ios-sim x86_64-apple-ios}; do
          installables+=(".#ios-$target" ".#ios-$target-deps")
        done
        nix build --accept-flake-config --no-link --print-out-paths "${installables[@]}" \
          | cachix push fedimint

    - name: Compress iOS binaries
      id: compress
      shell: bash
      working-directory: js/react-native/react-native-bindings
      run: |
        nix develop ../../../ -c zip -9 -r ios-artifacts.zip FedimintReactNativeBindingsFramework.xcframework
        IOS_SHA256=$(shasum -a 256 ios-artifacts.zip | cut -d' ' -f1)
        IOS_SIZE=$(du -h ios-artifacts.zip | cut -f1)
        echo "sha256=$IOS_SHA256" >> $GITHUB_OUTPUT
        echo "IOS_SHA256=$IOS_SHA256" >> $GITHUB_ENV
        echo "### iOS Artifacts" >> $GITHUB_STEP_SUMMARY
        echo "- Size: $IOS_SIZE" >> $GITHUB_STEP_SUMMARY
        echo "- SHA256: $IOS_SHA256" >> $GITHUB_STEP_SUMMARY

    - name: Upload Generated Bindings
      uses: actions/upload-artifact@v4
      with:
        name: generated-bindings-ios
        path: |
          js/react-native/react-native-bindings/src
          js/react-native/react-native-bindings/lib
          js/react-native/react-native-bindings/cpp
          js/react-native/react-native-bindings/ios-artifacts.zip
        if-no-files-found: error
